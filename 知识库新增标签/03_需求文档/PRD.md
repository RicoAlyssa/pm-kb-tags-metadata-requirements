# PRD：知识库新增标签

状态：评审中草稿（平台、瀚海与竞品已采证；新增标签接口设计、计费归属和正式原型待评审）

版本：v0.5
更新日期：2026-07-14
负责人：PM 待确认
关联项目总脑：`../00_项目总脑.md`

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 需求名称 | 知识库新增标签 |
| 需求 ID | KB-TAG |
| 当前阶段 | 平台现状已回填 + PRD 初评 + 新增接口设计 |
| 证据索引 | `../02_竞品调研/evidence/evidence-index.md` |
| 接口索引 | `../06_接口/接口清单.md` |
| 原型状态 | HTML 最小改动交互原型已完成；Pencil 按用户要求暂停 |
| 评审状态 | 等产品/业务平台/后端/前端/QA 评审 |

## 1. 背景与问题

一个账号下可能存在多个业务知识库；同一知识库内也可能混有财务、销售、研发等不同使用范围的文档。当前缺少稳定标签信号，导致：

- AI 应用难以在多个知识库中先选择正确检索对象；
- 知识库的项目、部门、成本中心等计费归属缺少可配置入口；
- 检索时无法在文档/切片进入召回前，基于业务标签做过滤。

本需求必须明确分为两类标签：

| 对象 | 作用 | 是否涉及计费 | 管理边界 |
|---|---|---|---|
| 知识库级标签 | 给知识库分类，用于应用/AI 路由、项目/业务归属和计费对接 | 是 | 需与业务/应用平台明确主数据、同步和计费归属 |
| 文档级标签 | 给文档分类，用于检索前过滤文档及切片 | 否 | 知识库侧管理，和权限系统分层 |

## 2. 目标、非目标与成功指标

### 2.1 目标

| ID | 目标 | 衡量方式 |
|---|---|---|
| TAG-G-001 | 管理员能分别管理知识库级标签和文档级标签 | 控台入口、列表、创建、编辑、删除、绑定链路可用 |
| TAG-G-002 | 应用/AI 能按知识库级标签选择候选知识库 | 检索/应用配置中可按标签过滤知识库，接口有明确表达 |
| TAG-G-003 | 文档级标签能在检索前过滤文档/切片 | 不符合标签条件的文档切片不进入召回候选集 |
| TAG-G-004 | 知识库级标签能承载计费归属 | 业务平台/计费系统能识别项目/部门/成本中心等归属 |

### 2.2 非目标

- 不用标签替代完整 RBAC/ABAC 权限系统。
- 不把文档级标签纳入计费。
- 不把元数据需求合并到标签需求；二者可共享过滤链路，但产品对象独立。
- 未确认接口前，不写死后端字段名、错误码或路径。

### 2.3 与元数据的边界

- 文档级标签是轻量分类与范围过滤对象，不配置字段类型、取值方法、是否参与模型回复等字段 Schema。
- 元数据字段定义、类型校验、内置字段、参与检索/模型回复开关归“知识库新增元数据”需求。
- 两者可复用底层过滤链路，但前端控件必须区分：标签使用 chip 选择器，元数据使用字段条件表达式。
- 知识库级标签可涉及项目、业务归属和计费同步；文档级标签与元数据均不直接承载计费归属。

### 2.4 成功指标（待基线）

- 标签变更后，在约定生效时间内影响检索候选集。
- 知识库级标签与计费归属无冲突；常态页面不展示“已同步”成功态，仅在同步中、失败或待处理时提示。
- 标签删除/改名不会造成不可解释的检索或计费结果。
- 管理员可追踪某标签影响哪些知识库、文档和应用。

## 3. 用户、角色与场景

| 角色 | 场景 | 权限要求 |
|---|---|---|
| 星流管理员 | 管理知识库级/文档级标签及高风险操作 | 复用 IAM `AICPFullAccess` 或等价自定义策略 |
| 业务开发人员 | 给可管理知识库的文档打标、配置检索过滤 | 复用 IAM `AICPMemberAccess`，按具体 Action 鉴权 |
| 只读人员 | 查看知识库、文档标签和检索配置 | 复用 IAM `AICPConsoleReadOnlyAccess`，不显示或禁用写操作 |
| 应用配置人员 | 在应用/工作流中选择知识库范围和文档标签条件 | 复用现有应用权限与知识库可见范围 |
| 计费/运营人员 | 查看知识库级标签对应的项目和计费归属 | 复用企业/项目/费用权限，不新增星流角色 |

> 金山云 IAM 的“角色”用于可信实体和临时授权；用户权限通过子用户/用户组绑定策略实现。本需求不新建产品内角色体系。

| 操作 | FullAccess | MemberAccess | ConsoleReadOnly | 自定义策略 |
|---|---|---|---|---|
| 查看标签和过滤配置 | 允许 | 允许 | 允许 | 按 Action |
| 文档打标、配置过滤 | 允许 | 允许 | 禁止 | 按 Action |
| 标签定义维护 | 允许 | 允许 | 禁止 | 按 Action |
| 知识库级标签及计费归属 | 允许 | 按项目/费用权限 | 只读 | 按 Action + 项目/费用权限 |
| 删除被引用标签 | 允许并二次确认 | 按 Action | 禁止 | 按 Action |

策略接入：`AICPFullAccess`/`AICPMemberAccess` 的通配授权需复核显式拒绝项；`AICPConsoleReadOnlyAccess` 必须加入新增查询 Action，不能仅依赖前端隐藏按钮。

## 4. 范围与优先级

| 优先级 | 范围 | 说明 |
|---|---|---|
| P0 | 知识库级标签 CRUD、绑定/解绑、列表筛选、计费归属字段/异常同步提示 | 计费归属模型待业务平台确认 |
| P0 | 文档级标签 CRUD、文档绑定/解绑、批量打标、检索过滤表达 | 不涉及计费 |
| P0 | 操作权限、删除影响提示、基础错误反馈、审计字段 | 字段以接口为准 |
| P1 | 标签使用量、引用对象列表、导入/导出、批量跨库操作 | 依赖 P0 数据模型 |
| P1 | 应用编排中按标签动态选择知识库 | 依赖应用平台入口 |
| P2 | 标签推荐、自动打标、标签治理报表 | 本期不作为上线阻塞 |

### 4.1 平台现状与最小改造

| 现有能力 | 处理方案 |
|---|---|
| 星流已有知识库列表与详情 | 不新增一级导航；标签入口嵌入现有“基本信息/知识库设置/文档信息/知识库检索” |
| 星流已有 IAM 只读、业务开发、全量权限策略 | 不新增角色；新增标签 Action 并纳入现有策略，前端按鉴权结果控制操作 |
| 星流已有项目归属，金山云已有全局标签管理 | 知识库级标签优先复用项目/资源标签设施；先确认知识库能否作为可绑定资源 |
| 星流 OpenAPI 已有文档导入、管理、检索 | 保持现有接口兼容；新增标签赋值与过滤能力，不重做文档接口 |
| 瀚海已有元数据与过滤底层 | 标签可复用其索引过滤引擎，但标签对象和元数据 Schema 仍独立 |
| 火山、阿里均支持打标和检索过滤 | P0 覆盖导入后编辑、批量打标、包含/不包含及 AND/OR |

## 5. 信息架构与主流程

| 现有页面 | 新增内容 |
|---|---|
| 新建知识库 | 保留名称、描述、所属项目、规格与费用栏；在所属项目后增加可选的知识库级标签，不在创建页定义文档标签 Schema |
| 导入文档 | 保留数据源、上传文件、解析方式、分段设置和底部操作栏；在上传文件后增加文档标签，应用到本次上传文档 |
| 知识库列表/基本信息 | 展示、筛选和编辑知识库级标签；保留所属项目 |
| 知识库设置 | 仅维护轻量文档标签池：标签名称、绑定文档数、过滤引用；不展示字段类型、取值方法或模型回复开关 |
| 文档信息 | 文档标签 chip、单文档编辑、批量添加/移除；不展示元数据字段值 |
| 知识库检索 | 标签 chip 选择器及召回结果验证；候选来自标签池，不展示字段、操作符和值 |
| 示例代码 | 展示新增过滤参数，保持旧请求兼容 |

### 5.1 知识库级标签主流程

1. 管理员新建知识库时，可在现有“所属项目”后选填已有知识库级标签；不填不阻断创建。
2. 创建后可继续在知识库列表或基本信息中编辑知识库级标签。
3. 创建标签并选择业务归属类型（项目/部门/成本中心等，`TO_CONFIRM`）。
4. 绑定一个或多个知识库。
5. 系统同步业务/应用平台；成功时只提示保存完成，不常驻展示“已同步”，同步中或失败时展示原因与重试入口。
6. 应用配置或 AI 路由按知识库级标签选择检索范围。

### 5.2 文档级标签主流程

1. 业务开发人员在现有“导入文档”页上传文件，并可选填文档标签；标签应用到本次上传的全部文档。
2. 导入后可在现有“文档信息”中选择单个或多个文档，继续添加或移除标签。
3. 保存后系统更新文档标签索引。
4. 检索请求携带文档级标签条件。
5. 系统先过滤候选文档/切片，再执行召回。

## 6. 功能需求

### 6.1 知识库级标签

| ID | 需求 | 优先级 | 行为 | 异常/边界 | 验收 |
|---|---|---|---|---|---|
| TAG-KB-001 | 查看知识库级标签列表 | P0 | 展示名称、绑定知识库数、业务归属、更新时间；常态不展示成功同步状态 | 无标签显示空态；无权限隐藏编辑操作；同步异常时展示异常提示 | AC-TAG-001 |
| TAG-KB-002 | 创建知识库级标签 | P0 | 输入名称，选择/填写业务归属信息；保存后进入列表 | 重名、超长、非法字符、业务归属缺失需阻断 | AC-TAG-002 |
| TAG-KB-003 | 编辑知识库级标签 | P0 | 支持修改名称和业务归属；展示影响范围 | 已关联计费归属的标签修改需二次确认；同步失败可重试 | AC-TAG-003 |
| TAG-KB-004 | 删除知识库级标签 | P0 | 删除前展示绑定知识库、应用引用、计费影响 | 被计费/应用引用时按规则阻断或要求解除引用 | AC-TAG-004 |
| TAG-KB-005 | 绑定/解绑知识库 | P0 | 一个知识库可绑定多个知识库级标签；支持批量 | 部分成功需明确回滚/保留策略 | AC-TAG-005 |
| TAG-KB-006 | 按标签筛选知识库 | P0 | 控制台、应用配置或检索路由可按标签选知识库 | 标签不存在、无权限、同步未完成需有提示 | AC-TAG-006 |
| TAG-KB-007 | 计费归属同步 | P0 | 标签变更触发业务平台/计费归属更新 | 同步失败、延迟、生效时间需可见 | AC-TAG-007 |
| TAG-KB-008 | 新建知识库时选择知识库级标签 | P0 | 在现有新建知识库页“所属项目”后增加可选的知识库级标签选择；仅允许选择已有知识库级标签，不在创建页新建或维护文档级标签；不填不阻断创建；“立即购买/创建”沿用现有购买与创建链路 | 标签失效、无权限、业务/计费同步未确认时需明确提示；不得默认展示文档级标签或元数据入口 | AC-TAG-008 |

### 6.2 文档级标签

| ID | 需求 | 优先级 | 行为 | 异常/边界 | 验收 |
|---|---|---|---|---|---|
| TAG-DOC-001 | 查看文档级标签 | P0 | 在知识库设置展示标签定义和使用量，在文档信息展示每篇文档标签 | 无标签显示空态；只读策略不可编辑 | AC-TAG-101 |
| TAG-DOC-002 | 创建/编辑文档级标签 | P0 | 创建轻量分类标签，供导入、单文档和批量绑定使用；不配置字段类型、取值方法或参与模型回复开关 | 重名、超长、非法字符需阻断；强类型字段归元数据 | AC-TAG-102 |
| TAG-DOC-003 | 单文档打标 | P0 | 文档详情可新增/移除标签，保存后反馈成功 | 文档不存在、索引中、无权限需提示 | AC-TAG-103 |
| TAG-DOC-004 | 批量文档打标 | P0 | 文档列表多选后添加/移除标签 | 部分文档失败需展示失败清单 | AC-TAG-104 |
| TAG-DOC-005 | 检索前标签过滤 | P0 | 以条件组配置包含/不包含及 AND/OR，先过滤文档/切片再召回 | 空条件不提交；标签失效时阻断保存并提示 | AC-TAG-105 |
| TAG-DOC-006 | 权限边界 | P0 | 标签可作为过滤条件，但不替代权限系统最终鉴权 | 无权限用户不能通过标签绕过文档权限 | AC-TAG-106 |
| TAG-DOC-007 | 标签索引异常提示 | P0 | 保存后成功态只做轻提示；仅在处理中或失败时展示明确提示，并记录失败原因 | 生效失败可重试；旧条件不得扩大权限范围 | AC-TAG-107 |
| TAG-DOC-008 | IAM 权限复用 | P0 | 标签查看/定义/赋值/过滤分别映射星流 Action，并纳入现有 IAM 策略 | 403 保留页面上下文并提示申请权限 | AC-TAG-108 |

## 7. 交互截图表

正式截图需来自后续 Pencil/HTML 验证；当前先给出区域、状态与交互要求。

| 区域/状态截图 | 页面或区域说明 | 显示逻辑 | 操作与交互逻辑 | 异常/边界 | 证据或原型来源 |
|---|---|---|---|---|---|
| `STATE-TAG-KB-CREATE-PAGE.png` | 现有“新建知识库”页 | 保留名称、描述、项目、三档规格和底部费用；增加可选知识库标签 | 选标签、切换规格、立即购买；创建后标签与知识库一起保存 | 名称必填；标签选填；计费归属未确认不得伪造同步成功 | `../05_设计素材/html-prototype/preview-tags-create-v1.png` |
| `STATE-TAG-DOC-IMPORT.png` | 现有“导入文档”页 | 保留上传、解析与分段配置；上传文件后显示文档标签选择 | 选择已有标签后确认导入；导入后可单篇或批量修改 | 标签选填；无权限、标签失效、导入失败时保留页面输入 | `../05_设计素材/html-prototype/preview-tags-import-v1.png` |
| `STATE-TAG-KB-LIST.png` | 现有知识库列表/基本信息 | 卡片或基本信息显示知识库级标签、所属项目；不展示“已同步”成功态 | 按标签筛选；有权限时编辑标签 | 只读、同步失败、标签失效 | 复用星流现有页面 |
| `STATE-TAG-KB-CREATE.png` | 知识库标签编辑弹窗/抽屉 | 当前标签和可选标签 | 添加/移除后保存；保存中防重复提交 | 重名、非法字符、同步失败 | 复用现有项目/资源标签交互 |
| `STATE-TAG-KB-BIND.png` | 给知识库绑定标签 | 展示已绑标签和可选标签；可搜索 | 多选标签后保存；保存成功刷新列表和绑定数 | 部分失败、标签被删除、知识库无权限 | 参考 TAG-EV-0003；我方待原型 |
| `STATE-TAG-KB-BILLING.png` | 计费归属提示区 | 仅知识库级标签显示；文档级标签不显示 | 展示项目/部门/成本中心；同步中或失败时展示原因和重试入口，成功态不常驻 | 业务平台不可用、计费归属冲突、待同步 | TO_CONFIRM：业务平台方案 |
| `STATE-TAG-DOC-LIST.png` | “知识库设置”内文档标签区 | 展示名称、绑定文档数、过滤引用；不展示字段类型 | 新建、编辑、删除；不提供引用详情入口 | 被引用删除、只读；不出现计费字段或元数据字段 Schema | 复用星流设置页 |
| `STATE-TAG-DOC-BATCH.png` | 文档列表批量打标 | 多选文档后出现批量设置标签入口 | 添加/移除标签，提交后显示成功/失败清单 | 部分文档无权限、索引中、已删除 | 竞品入口已验证；我方待原型 |
| `STATE-TAG-DOC-DETAIL.png` | 文档详情标签区 | 展示当前文档标签，可编辑 | 添加已有标签或新建标签后绑定；保存/取消 | 空态、无权限、标签被并发删除 | TO_CONFIRM：Pencil 正式原型 |
| `STATE-TAG-RETRIEVAL.png` | 检索/应用配置过滤条件 | 文档级标签以 chip 选择器呈现，分“包含标签/排除标签”；不展示字段、操作符、值三段式条件 | 直接选择标签 chip，切换匹配全部/任一已选标签，测试召回 | 条件为空、标签失效、无可见知识库/文档；元数据字段不得混入标签候选 | 火山/阿里过滤交互已验证；我方接口待确认 |

## 8. 接口与数据

### 8.0 本版后端最小 Action 契约

完整契约见：`../06_接口/后端接口契约-最小改动.md`。

本版不再只写“待后端确认接口”，而是按当前星流平台 `Action + Version=2025-11-14` 风格给出建议 Action。知识库级标签和文档级标签分开；文档级标签不含计费字段；知识库级标签涉及业务/计费归属。

| 前端能力 | 建议 Action / 改动 | 最小改动说明 |
|---|---|---|
| 查询知识库级标签 | `DescribeKnowledgeBaseTags` | 新增 Action；可按 `DatasetId` 返回已绑定标签 |
| 新建/编辑/删除知识库级标签 | `CreateKnowledgeBaseTag` / `ModifyKnowledgeBaseTag` / `DeleteKnowledgeBaseTag` | 新增 Action；包含 `BusinessOwner`，同步异常只展示异常态 |
| 绑定知识库级标签 | `ModifyKnowledgeBaseTagBindings` | 新增 Action；支持 Add/Remove/Set，部分失败返回明细 |
| 文档标签池 | `DescribeDocumentTags` | 新增 Action；展示名称、绑定文档数、过滤引用 |
| 新建/编辑/删除文档标签 | `CreateDocumentTag` / `ModifyDocumentTag` / `DeleteDocumentTag` | 新增 Action；不含字段类型、取值方式或模型回复开关 |
| 单篇/批量文档打标 | `BatchModifyDocumentTags` | 新增 Action；部分成功返回失败清单 |
| 导入时打标 | `ImportDocuments.DocumentTagIds` | 兼容扩展；不传时旧行为不变 |
| 文档列表/详情标签展示 | `DescribeDocuments/DescribeDocument.IncludeDocumentTags` | 兼容扩展；默认不返回，旧调用不受影响 |
| 标签检索过滤 | `RetrieveKnowledge.RetrievalModel.TagFilter` | 兼容扩展；包含标签、排除标签、匹配全部/任一 |

关键语义：

- 文档级标签只做分类和检索过滤，不承载计费。
- 知识库级标签成功同步态不常驻展示；只展示同步中/失败。
- 文档权限先收敛候选范围，标签过滤只能继续缩小范围。
- 批量打标采用部分成功：`SucceededItems[] + FailedItems[]`。

### 8.1 已有证据

- 星流 OpenAPI `2025-11-14` 已覆盖 `ImportDocuments`、`DescribeDocuments`、`DescribeDocument`、`DeleteDocument`、`ReindexDocuments`、`RetrieveKnowledge` 等主链路。
- `ImportDocuments` 当前没有标签字段，`RetrieveKnowledge.RetrievalModel` 当前没有标签过滤字段。
- 星流已有 IAM 系统策略；本需求只增加权限 Action，不新增角色模型。
- 瀚海未见独立标签接口，但其元数据过滤链路可复用索引与条件执行能力。
- 火山、阿里均已验证文档打标和检索过滤，属于我方应补齐的共同能力。

### 8.2 最小接口改造

| 现有接口/设施 | 最小改造 | 兼容要求 |
|---|---|---|
| 金山云资源标签/项目 | 支持知识库级标签绑定，或由星流适配为可标签资源 | 先验证知识库资源类型；不能复用时再新增星流标签存储 |
| `ImportDocuments` | P0 增加可选文档标签数组，用于导入时给本批文档赋值 | 不传标签时行为完全不变 |
| 文档列表/详情接口 | 返回文档标签；增加单文档和批量添加/移除能力 | 保留现有分页、状态和重索引流程 |
| `RetrieveKnowledge.RetrievalModel` | 增加可选标签条件组 | 旧请求无条件时保持原检索结果 |
| IAM 系统策略 | 增加查看、定义、赋值、过滤等标签 Action | 全量/成员/只读策略按现有语义纳入；自定义策略可单独授权 |
| 操作审计 | 记录标签定义、绑定、解绑、删除和过滤配置变化 | 复用金山云操作审计，不建独立审计系统 |

## 9. 异常与边界

| 场景 | 预期处理 |
|---|---|
| 标签重名 | 阻断保存，错误定位到名称字段 |
| 标签被引用时删除 | 展示引用范围；知识库级还需展示应用/计费影响 |
| 业务平台同步失败 | 保存状态明确；允许重试或回滚，需有审计 |
| 文档级标签过滤与权限冲突 | 权限优先；标签过滤不能扩大可见范围 |
| 标签变更后索引未生效 | 展示“生效中/失败/最近更新时间”，检索测试给出解释 |
| 批量打标部分失败 | 保留成功项并展示失败清单，或按接口约定全量回滚 |
| 并发编辑 | 保存时校验版本或最后更新时间，冲突需提示刷新 |

## 10. 验收标准

| ID | 对应需求 | 验收描述 |
|---|---|---|
| AC-TAG-001 | TAG-KB-001 | 给定管理员进入现有知识库列表或基本信息，则能看到标签和业务归属，且常态不展示“已同步”成功态 |
| AC-TAG-002 | TAG-KB-002 | 给定名称合法且业务归属完整，当创建标签，则保存成功并可绑定知识库 |
| AC-TAG-003 | TAG-KB-003 | 给定标签已被知识库引用，当编辑名称或业务归属，则系统展示影响并记录审计 |
| AC-TAG-004 | TAG-KB-004 | 给定标签关联应用或计费对象，当删除时，系统按规则阻断或要求解除引用 |
| AC-TAG-005 | TAG-KB-005 | 给定多知识库和多标签，当批量绑定，则成功项与失败项可追踪 |
| AC-TAG-006 | TAG-KB-006 | 给定应用配置按标签选库，当测试检索，则只进入符合知识库级标签的知识库范围 |
| AC-TAG-007 | TAG-KB-007 | 给定标签业务归属变更，当保存成功，则页面只做成功轻提示；当业务平台同步中或失败时，才展示状态、原因与重试入口 |
| AC-TAG-008 | TAG-KB-008 | 给定用户在新建知识库页未选择标签，当提交创建，则创建链路与现有一致；给定选择了有效知识库级标签，则创建后知识库列表/基本信息展示该标签，且文档级标签不出现在创建表单 |
| AC-TAG-101 | TAG-DOC-001 | 给定有权限用户进入知识库设置，则能看到文档标签及绑定文档数，不出现计费信息、字段类型或模型回复开关 |
| AC-TAG-102 | TAG-DOC-002 | 给定合法名称，当创建文档级标签，则可在文档详情和批量打标中选择，且创建过程不要求选择字段类型 |
| AC-TAG-103 | TAG-DOC-003 | 给定单文档打标成功，当刷新文档详情，则标签仍存在并进入过滤索引 |
| AC-TAG-104 | TAG-DOC-004 | 给定批量打标部分失败，当保存完成，则展示失败文档及原因 |
| AC-TAG-105 | TAG-DOC-005 | 给定检索条件包含文档级标签，当召回执行，则不符合条件的文档切片不进入候选集 |
| AC-TAG-106 | TAG-DOC-006 | 给定用户无某文档权限，即使标签条件命中，该文档切片也不得返回 |
| AC-TAG-107 | TAG-DOC-007 | 给定标签变更已保存，当索引未更新或更新失败，则页面展示明确状态且检索不得扩大权限范围 |
| AC-TAG-108 | TAG-DOC-008 | 给定全量、业务开发和只读策略用户进入相同知识库，则页面操作和接口结果符合 IAM 授权，未授权写请求返回 403 |

## 11. 指标与发布

| 指标 | 说明 |
|---|---|
| 标签创建成功率 | 区分知识库级/文档级 |
| 标签绑定成功率 | 区分单个/批量、成功/部分失败 |
| 计费同步失败数 | 仅知识库级标签 |
| 标签过滤检索命中变化 | 记录过滤前后候选文档/切片数量 |
| 删除/改名失败原因分布 | 用于优化引用提示和操作流程 |

发布建议：

1. 灰度给内部测试租户。
2. 先开放文档级标签过滤和知识库级标签展示，再开放计费同步。
3. 计费链路必须有回滚和审计后再进入正式商用。

## 12. 决策与待确认

| ID | 状态 | 问题/决策 | 负责人 | 影响 |
|---|---|---|---|---|
| TAG-DEC-001 | CONFIRMED | 知识库级标签与文档级标签分开写 | 用户 | PRD、接口、验收双轨 |
| TAG-DEC-002 | CONFIRMED | 只有知识库级标签涉及计费 | 用户 | 文档级标签不得出现计费字段 |
| TAG-DEC-003 | CONFIRMED | 不新增产品内角色体系，复用星流 IAM 系统策略和自定义策略 | 星流控制台采证 | 权限、前端按钮、接口 Action |
| TAG-DEC-004 | CONFIRMED | 不新增一级导航，入口嵌入现有知识库列表/设置/文档信息/检索 | 星流控制台采证 + 最小改造原则 | 信息架构和原型 |
| TAG-DEC-005 | CONFIRMED | 新建知识库页只暴露知识库级标签选择，不暴露文档级标签或元数据定义/赋值 | 用户当前 PRD（2026-07-14） | 前端原型、创建链路和验收边界 |
| TAG-DEC-006 | CONFIRMED | 文档级标签按轻量分类/过滤对象设计，不呈现元数据字段 Schema | 用户当前反馈 + 阿里/火山竞品边界 | HTML 原型、PRD 页面职责 |
| TAG-Q-001 | TO_CONFIRM | 知识库级标签计费归属模型是什么 | 产品+业务平台 | 阻塞知识库级标签 P0 定稿 |
| TAG-Q-002 | CONFIRMED | IAM/文档权限先收敛可见范围，标签只能继续缩小范围 | 产品+后端 | 检索/鉴权验收 |
| TAG-Q-003 | CONFIRMED | 星流现有 OpenAPI 与瀚海均无独立标签接口，需要新增星流适配能力 | 控制台 + API Explorer + Apifox | 接口和前端实现 |
| TAG-Q-004 | TO_CONFIRM | 标签变更对检索索引的生效时间 | 后端 | 阻塞异常和验收 |
| TAG-Q-005 | TO_CONFIRM | 知识库级标签的业务平台/计费接口、主数据源和同步方向 | 业务平台+后端 | 阻塞知识库级标签正式原型 |
| TAG-Q-006 | TO_CONFIRM | 金山云资源标签能否绑定星流知识库资源 | 平台后端 | 决定知识库级标签复用或新增适配层 |

## 13. 评审记录

详见 `评审记录.md`。当前版本可进入内部初评，但不能作为研发最终排期依据。
