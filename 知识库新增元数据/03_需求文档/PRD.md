# PRD：知识库新增元数据

状态：评审中草稿（平台、瀚海与竞品已采证；星流适配、完整校验和正式原型待评审）

版本：v0.5
更新日期：2026-07-14
负责人：PM 待确认
关联项目总脑：`../00_项目总脑.md`

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 需求名称 | 知识库新增元数据 |
| 需求 ID | KB-META |
| 当前阶段 | 平台现状已回填 + PRD 初评 + 星流适配设计 |
| 证据索引 | `../02_竞品调研/evidence/evidence-index.md` |
| 接口索引 | `../06_接口/接口清单.md` |
| 原型状态 | HTML 最小改动交互原型已完成；Pencil 按用户要求暂停 |
| 评审状态 | 等产品/后端/前端/QA/检索负责人评审 |

## 1. 背景与问题

瀚海 OPEN-API 已出现元数据能力，但金山云当前星流平台 API Explorer 和控制台尚未暴露同等能力。管理员无法在页面上定义、查看或修改文档的自定义原始信息，应用配置人员也无法稳定地用元数据做检索过滤。

典型场景：

- 文档需要记录来源 URL、部门、发布日期、文档类型等原始信息；
- 检索时需要先按这些信息缩小候选文档/切片，再做语义召回；
- 客户希望按自己的 key 管理文档信息，而不是只能依赖文件名或上传时间。

## 2. 目标、非目标与成功指标

### 2.1 目标

| ID | 目标 | 衡量方式 |
|---|---|---|
| META-G-001 | 管理员能在控制台管理元数据定义 | 可查看、新增、编辑、删除字段定义 |
| META-G-002 | 管理员能给文档填写或修改元数据值 | 支持文档详情单个编辑和文档列表批量编辑 |
| META-G-003 | 应用/召回测试能配置元数据过滤条件 | 检索请求能携带 metadata filter 并影响候选文档 |
| META-G-004 | 前后端字段与底层接口一致 | 字段映射表覆盖请求、响应、错误和边界 |

### 2.2 非目标

- 不把元数据与标签合并。
- 不用元数据直接替代权限系统或计费系统。
- 不在未确认接口前自定义字段类型、错误码或检索表达式。
- 本期不做自动抽取/智能推荐，除非后端或竞品证据证明已有低成本能力。

### 2.3 与标签的边界

- 元数据是字段 Schema + 文档属性值，不作为业务分类 chip 或计费归属标签。
- 元数据定义面向用户只体现字段名、类型和文档值，不暴露“字段用途”“参与检索”“参与模型回复”等内部开关。
- 文档信息页只展示/编辑该文档的元数据字段和值；标签 chip、标签池、计费归属不进入元数据需求。
- 检索页可复用底层过滤链路，但前端控件必须区分：元数据使用字段条件表达式，标签使用 chip 选择器。

### 2.4 成功指标（待基线）

- 管理员可在控台完成元数据定义和赋值，不依赖手工 API 调用。
- 元数据更新后在约定时间内影响检索过滤。
- 批量更新失败时，用户能知道哪些文档失败以及失败原因。
- 删除字段定义或字段值前，影响范围清晰可见。

## 3. 用户、角色与场景

| 角色 | 场景 | 权限要求 |
|---|---|---|
| 星流管理员 | 管理元数据定义、删除和高风险操作 | 复用 IAM `AICPFullAccess` 或等价自定义策略 |
| 业务开发人员 | 编辑文档元数据、配置检索过滤 | 复用 IAM `AICPMemberAccess`，按具体 Action 鉴权 |
| 只读人员 | 查看字段定义、文档值和过滤配置 | 复用 IAM `AICPConsoleReadOnlyAccess`，不允许写操作 |
| 应用配置人员 | 在应用/召回测试中配置元数据条件 | 复用现有应用权限与知识库可见范围 |
| 后端/QA | 验证瀚海适配、IAM、错误码和索引生效 | 测试环境权限 |

> 本需求不新增产品内角色。金山云 IAM 子用户/用户组绑定策略负责授权；瀚海“空间管理员”仅作为底层空间权限参考。

| 操作 | FullAccess | MemberAccess | ConsoleReadOnly | 自定义策略 |
|---|---|---|---|---|
| 查看定义、文档值和过滤配置 | 允许 | 允许 | 允许 | 按 Action |
| 新增、编辑、文档赋值、配置过滤 | 允许 | 允许 | 禁止 | 按 Action |
| 删除被引用定义 | 允许并二次确认 | 按 Action | 禁止 | 按 Action |
| 查看更新记录和异常提示 | 允许 | 允许 | 允许 | 按 Action |

策略接入：`AICPFullAccess`/`AICPMemberAccess` 的通配授权需复核显式拒绝项；`AICPConsoleReadOnlyAccess` 必须加入新增查询 Action，不能仅依赖前端隐藏按钮。

## 4. 范围与优先级

| 优先级 | 范围 | 说明 |
|---|---|---|
| P0 | 元数据定义列表、新增、编辑、删除 | 复用瀚海 `name/type/id/count` 数据模型 |
| P0 | 文档详情查看/编辑元数据值 | 单文档入口必须可用 |
| P0 | 文档列表批量编辑元数据值 | 支持多文档赋值、清空、失败明细 |
| P0 | 检索/召回测试中配置元数据过滤 | 明确 filter 表达式和生效链路 |
| P0 | 接口字段映射、错误反馈、权限 | 依赖 Apifox 完整 schema |
| P1 | 内置字段开关、引用关系、删除影响详情 | 瀚海已有 `built_in_field_enabled`，交互和权限待确认 |
| P1 | 元数据定义导入/导出、批量清空 | 依赖 P0 稳定；文档导入完成后再赋值 |
| P2 | 自动抽取、正则/LLM、参与模型上下文开关 | 阿里已实现，但瀚海当前接口未提供 |

### 4.1 平台现状与最小改造

| 现有能力 | 处理方案 |
|---|---|
| 星流已有新建知识库单页表单和购买流程 | 完全保持现状；创建知识库时不定义元数据 |
| 星流已有知识库详情页签 | 不新增一级导航；定义放“知识库设置”，赋值放“文档信息”，过滤放“知识库检索” |
| 星流已有 IAM 只读、业务开发、全量策略 | 不新增角色；新增元数据 Action 并纳入现有策略 |
| 星流已有导入、文档管理和检索 OpenAPI | 保持 Action 风格和旧请求兼容，只扩展缺失字段/接口 |
| 瀚海已有定义 CRUD、批量赋值和过滤 | P0 直接复用其数据模型与服务能力，通过星流适配层暴露 |
| 火山、阿里都有强类型字段过滤 | P0 完成过滤闭环；更多类型和自动抽取按瀚海能力分期补齐 |

### 4.2 类型与过滤分期

| 阶段 | 类型/能力 | 依据 |
|---|---|---|
| P0 | `String` 定义、文档赋值、`==`、`!=`、`contains_any`、AND/OR | 瀚海接口示例与过滤说明已明确 |
| P1 | Number、Boolean、DateTime、List 及范围比较 | 火山/阿里均有同类能力；以瀚海实际枚举为准 |
| P2 | GeoPoint、正则/LLM 自动抽取、进入模型上下文开关 | 竞品增强项，瀚海当前接口未提供 |

## 5. 信息架构与主流程

| 现有页面 | 新增内容 |
|---|---|
| 新建知识库 | 完全保持现状，不增加元数据定义 |
| 导入文档 | 完全保持现有上传、解析方式和分段设置，不增加文档元数据值 |
| 知识库设置 | 元数据定义列表、新增、编辑、删除、填写文档数；不暴露字段用途开关 |
| 文档信息 | 元数据摘要列、单文档编辑、批量赋值/清空；展示字段名和值，不展示标签 chip |
| 知识库检索 | 元数据字段条件表达式和过滤结果验证；字段候选来自元数据定义，不混入标签候选 |
| 示例代码 | 展示新增过滤参数，旧请求保持兼容 |

### 5.1 定义管理流程

1. 管理员先按现有流程创建知识库。
2. 创建完成后进入“知识库设置 > 元数据定义”。
3. 页面展示名称、类型和使用文档数；字段 ID 和“已生效”成功态不在列表中展示。
4. 管理员新增元数据定义，填写名称并选择后端支持的类型。
5. 编辑时仅允许修改名称；类型锁定。字段被引用时展示影响范围。
6. 删除字段定义前，提示该字段及所有文档值的影响范围。

### 5.2 文档赋值流程

1. 管理员按现有流程导入文档，导入页不填写元数据值。
2. 导入完成后进入文档详情，查看当前文档元数据值。
3. 点击编辑后，可新增字段值、修改值、清空值或移除该文档上的字段。
4. 文档列表多选后，可进入批量元数据编辑器，支持只更新指定字段、应用到全部选中文档、清空值和部分失败反馈。

### 5.3 检索过滤流程

1. 应用配置人员在召回测试或应用知识库节点中添加元数据过滤条件。
2. 选择字段、运算符和值。
3. 系统按条件过滤候选文档/切片，再执行召回。
4. 测试结果显示过滤条件、候选数量变化和命中文档元数据。

## 6. 功能需求

| ID | 需求 | 优先级 | 行为 | 异常/边界 | 验收 |
|---|---|---|---|---|---|
| META-REQ-001 | 查看元数据定义列表 | P0 | 展示名称、类型、填写文档数；不展示字段用途类开关和“已生效”成功态 | 无字段显示空态；只读策略不可编辑 | AC-META-001 |
| META-REQ-002 | 新增元数据定义 | P0 | 填写名称并选择后端支持的类型 | 重名、非法名称、类型缺失、超限阻断 | AC-META-002 |
| META-REQ-003 | 编辑元数据定义 | P0 | 复用瀚海能力，仅修改名称；类型只读 | 被使用或引用时展示影响 | AC-META-003 |
| META-REQ-004 | 删除元数据定义 | P0 | 删除前展示影响范围，确认后删除字段和相关值 | 被检索配置引用时需阻断或提示迁移 | AC-META-004 |
| META-REQ-005 | 文档详情查看/编辑元数据 | P0 | 展示当前文档值；编辑后保存到后端 | 值类型不匹配、文档不存在、无权限 | AC-META-005 |
| META-REQ-006 | 文档列表批量编辑元数据 | P0 | 多选文档后批量添加/更新/清空字段值 | 部分失败需失败清单；覆盖/部分更新语义待确认 | AC-META-006 |
| META-REQ-007 | 元数据检索过滤 | P0 | 通过已定义字段、操作符和值组成字段条件组，支持 AND/OR；字段候选不混入文档标签 | 类型与操作符不匹配、字段失效、空条件时阻断保存 | AC-META-007 |
| META-REQ-008 | 内置字段展示与开关 | P1 | 内置字段与自定义字段分组展示；读取瀚海 `built_in_field_enabled` | 开关接口和权限未确认时仅展示状态 | AC-META-008 |
| META-REQ-009 | 审计与异常提示 | P0 | 展示最近更新时间、更新人；成功态不常驻展示，仅在索引延迟或失败时提示 | 索引延迟、失败、并发冲突 | AC-META-009 |
| META-REQ-010 | IAM 权限复用 | P0 | 定义查看/维护、文档赋值、过滤配置分别映射星流 Action | 403 保留页面上下文并提示申请权限 | AC-META-010 |

## 7. 交互截图表

正式截图需来自后续 Pencil/HTML 验证；当前先给出区域、状态与交互要求。

| 区域/状态截图 | 页面或区域说明 | 显示逻辑 | 操作与交互逻辑 | 异常/边界 | 证据或原型来源 |
|---|---|---|---|---|---|
| `STATE-META-DEFINITION-LIST.png` | “知识库设置”内元数据区 | 展示名称、类型、填写文档数、内置/自定义；不展示字段用途开关 | 搜索、新增、编辑、删除 | 空态、只读、加载失败；不得展示为标签 chip | 复用星流设置页 + 瀚海字段模型 |
| `STATE-META-CREATE.png` | 新增元数据弹窗 | 名称、类型 | 保存/取消 | 重名、非法名称、类型缺失 | 复用瀚海 `name/type` |
| `STATE-META-EDIT.png` | 编辑元数据弹窗 | 名称可编辑，类型只读 | 编辑后保存；查看影响范围 | 被检索配置引用、并发修改 | 复用瀚海更新接口仅支持 `name` |
| `STATE-META-DELETE.png` | 删除定义确认 | 展示使用文档数、引用配置、删除后影响 | 二次确认删除 | 被引用阻断、删除失败、无权限 | 我方待接口确认 |
| `STATE-META-DOC-DETAIL.png` | 文档详情元数据区 | 展示当前文档字段和值；空态显示添加入口；不展示标签 chip | 进入编辑、添加字段、改值、清空、保存 | 值类型错误、文档索引中、无权限 | 参考 META-EV-0005 |
| `STATE-META-BATCH.png` | “文档信息”批量元数据编辑器 | 多选文档后展示已定义字段和值 | 添加/更新/清空值后保存 | 部分失败、覆盖/部分更新 | 复用瀚海 `operation_data[]` |
| `STATE-META-RETRIEVAL.png` | “知识库检索”元数据过滤 | 展示“字段 + 操作符 + 值”的条件表达式；字段候选来自元数据定义 | 添加/删除字段条件并测试召回 | 字段失效、类型不匹配、无命中；文档标签不得混入元数据候选 | 复用瀚海 `metadata_filtering_conditions` |
| `STATE-META-ERROR.png` | 接口错误反馈 | 表单级 + 字段级错误展示 | 保留用户输入，支持重试 | 401/403/404/400/批量部分失败 | 待接口错误码确认 |

## 8. 接口与数据

### 8.1 已验证我方接口线索

接口证据分成两层：星流是对外兼容基线，瀚海是优先复用的底层实现。

| 星流现有能力 | 当前结论 |
|---|---|
| `ImportDocuments`（`2025-11-14`） | 支持解析、索引、结构化 Schema 等配置；没有自定义文档元数据赋值字段 |
| `DescribeDocuments` / `DescribeDocument` | 支持文档列表/详情；现有 `Metadata` 不是客户自定义字段定义 |
| `RetrieveKnowledge.RetrievalModel`（`2025-11-14`） | 支持检索、重排、TopK、阈值等；没有元数据过滤条件 |
| 文档删除、重索引、状态和历史 | 已有，新增元数据能力必须保持兼容 |
| IAM | 已有全量、业务开发、只读和自定义策略，本需求新增 Action 即可 |

| 能力 | 方法/路径 | 状态 |
|---|---|---|
| 新增元数据 | `POST /knowledge/openapi/v2/datasets/{dataset_id}/metadata` | 瀚海已见：请求 `name/type` |
| 更新元数据 | `PATCH /knowledge/openapi/v2/datasets/{dataset_id}/metadata/{metadata_id}` | 瀚海已见；当前只见改名字段 `name` |
| 删除元数据 | `DELETE /knowledge/openapi/v2/datasets/{dataset_id}/metadata/{metadata_id}` | 瀚海已见；删除影响待确认 |
| 查询元数据 | `GET /knowledge/openapi/v2/datasets/{dataset_id}/metadata` | 响应含 `doc_metadata[].id/name/type/count`、`built_in_field_enabled` |
| 更新文档元数据 | `POST /knowledge/openapi/v2/datasets/{dataset_id}/documents/metadata` | 已提取主要结构 |
| 检索元数据过滤 | `POST /knowledge/openapi/v2/datasets/{dataset_id}/retrieve` | 瀚海已见 `filter_expression` / `metadata_filtering_conditions`；当前平台 `RetrieveKnowledge` 未见同等字段 |

更新文档元数据已知结构：

- 路径参数：`dataset_id`，UUID，必填。
- 请求体：`operation_data[]`。
- 单项：`operation_data[].document_id`。
- 元数据列表：`operation_data[].metadata_list[]`，示例含 `id/name/value`。
- 已见响应：`200`（无响应体）、`400`、`401`、`403`、`404`。

### 8.2 星流适配原则

| 项目 | 需求结论 |
|---|---|
| 控制台调用 | 星流控制台通过适配层调用瀚海能力，不直接暴露瀚海开发环境地址 |
| 创建主接口 | 不修改；知识库创建完成后再调用元数据定义接口 |
| 对外 API | 保持星流 `Action + Version` 风格；Action 名称由后端评审确定 |
| 检索兼容 | 在 `RetrieveKnowledge.RetrievalModel` 增加可选条件；不传时行为不变 |
| 文档兼容 | 不修改 `ImportDocuments`；文档导入完成后使用独立批量更新能力赋值 |
| 权限 | 每项写操作映射 IAM Action；全量/成员/只读策略和自定义策略沿用现有机制 |
| 审计 | 复用金山云操作审计，记录定义和文档值变化 |

### 8.3 友商共同能力的增量要求

| 共同能力 | 本期处理 |
|---|---|
| 强类型字段参与过滤 | P0 以瀚海已支持类型为准；扩展类型后续补齐 |
| 文档值编辑与批量赋值 | P0 完成，复用瀚海批量结构 |
| AND/OR 条件过滤 | P0 完成，复用瀚海条件组 |
| 自动抽取、搜索/模型用途开关 | P2；瀚海当前接口无对应字段，不阻塞 P0 |

### 8.4 待确认接口语义

| 问题 | 影响 |
|---|---|
| `id/name/value` 的必填性、类型、空值语义 | 决定表单校验和清空行为 |
| 是否支持 `partial_update` 或覆盖更新 | 决定批量编辑器“只更新已有字段/应用到全部” |
| 批量更新是全量事务还是部分成功 | 决定失败清单和回滚 |
| 瀚海支持的完整类型和运算符枚举 | 决定表单控件和校验 |
| 星流适配层 Action 名称与版本 | 决定 API Explorer 和联调 |

## 9. 异常与边界

| 场景 | 预期处理 |
|---|---|
| 字段重名 | 阻断保存，错误定位到字段名 |
| 类型不可改 | 编辑页禁用类型选择并说明原因 |
| 删除字段定义 | 提示全局影响；若被检索配置引用需阻断或要求迁移 |
| 删除某文档字段值 | 只影响当前文档或选中文档，不删除字段定义 |
| 批量部分失败 | 展示失败文档、字段、原因；成功项是否保留以接口约定为准 |
| 值类型不匹配 | 字段级错误，不提交或提交后回显错误 |
| 检索字段被删除 | 召回测试/应用配置标红失效条件，要求重新选择 |
| 元数据更新未生效 | 显示生效中/失败/最后更新时间，召回测试给出提示 |
| 无权限 | 只读或隐藏编辑入口，接口 403 明确提示 |

## 10. 验收标准

| ID | 对应需求 | 验收描述 |
|---|---|---|
| AC-META-001 | META-REQ-001 | 给定用户进入知识库设置，则能看到名称、类型、填写文档数，且看不到字段用途类开关或“已生效”成功态 |
| AC-META-002 | META-REQ-002 | 给定合法名称和后端支持类型，当保存后，字段出现在列表和文档编辑器 |
| AC-META-003 | META-REQ-003 | 给定字段已被使用，当编辑时，类型不可改，名称修改后正确生效 |
| AC-META-004 | META-REQ-004 | 给定字段被文档或检索配置引用，当删除时，系统展示影响并按规则阻断/确认 |
| AC-META-005 | META-REQ-005 | 给定文档详情编辑元数据值，当保存成功并刷新，值仍正确展示 |
| AC-META-006 | META-REQ-006 | 给定批量更新多文档，当部分失败，则用户能看到失败文档和原因 |
| AC-META-007 | META-REQ-007 | 给定多个来自元数据定义且类型合法的 AND/OR 条件，当召回测试执行，则不符合条件的文档切片不进入候选集 |
| AC-META-008 | META-REQ-008 | 给定启用内置字段，当查看文档元数据，则系统字段按只读/自动生成规则展示 |
| AC-META-009 | META-REQ-009 | 给定元数据更新后索引未完成或失败，当立即测试召回，则页面显示延迟/失败提示；成功后不常驻展示“已生效” |
| AC-META-010 | META-REQ-010 | 给定全量、业务开发和只读策略用户进入相同知识库，则写操作和接口结果符合 IAM 授权，未授权请求返回 403 |

## 11. 指标与发布

| 指标 | 说明 |
|---|---|
| 元数据字段创建成功率 | 监控定义管理稳定性 |
| 文档元数据更新成功率 | 区分单文档/批量 |
| 批量部分失败比例 | 用于优化失败清单和重试 |
| metadata filter 使用次数 | 观察功能被应用配置采纳情况 |
| 过滤前后候选文档数量 | 验证过滤是否真实生效 |

发布建议：

1. 先开放只读定义列表和文档详情展示。
2. 再开放单文档编辑和批量编辑。
3. 最后开放应用/召回测试 metadata filter，并监控检索质量和性能。

## 12. 决策与待确认

| ID | 状态 | 问题/决策 | 负责人 | 影响 |
|---|---|---|---|---|
| META-DEC-001 | CONFIRMED | 元数据作为独立需求，不与标签合并 | 用户 | PRD、接口、验收独立 |
| META-DEC-002 | CONFIRMED | 元数据定义属于知识库级 Schema，文档填写已定义字段值 | 竞品证据 + 产品收敛 | 定义、赋值、过滤统一 |
| META-DEC-003 | CONFIRMED | P0 复用瀚海 `name/type` 定义、批量赋值和条件过滤，不照搬阿里抽取模板 | 瀚海控制台 + Apifox | 范围与接口 |
| META-DEC-004 | CONFIRMED | 不新增角色和一级导航，复用星流 IAM 与现有知识库页签 | 星流控制台采证 | 权限与信息架构 |
| META-DEC-005 | SUPERSEDED | 旧表述“创建知识库主接口需支持可选元数据定义”与 2026-07-14 当前 PRD 冲突，废弃 | 用户当前 PRD | 防止误改创建页/创建接口 |
| META-DEC-006 | CONFIRMED | 创建知识库主接口和创建页不增加元数据定义；元数据定义在创建后进入知识库设置维护，文档元数据在导入完成后于文档信息赋值 | 用户当前 PRD（2026-07-14）+ 瀚海调用顺序 | 创建页、接口兼容、原型边界 |
| META-DEC-007 | CONFIRMED | 元数据按字段 Schema + 文档属性值设计，UI 必须和文档标签区分 | 用户当前反馈 + 阿里 Meta 模板证据 | HTML 原型、PRD 页面职责 |
| META-DEC-008 | CONFIRMED | 不向用户暴露字段用途、参与检索、参与模型回复等内部用途配置 | 用户当前反馈（2026-07-14） | 元数据定义弹窗、列表列项、验收口径 |
| META-Q-002 | TO_CONFIRM | 支持哪些值类型、名称规则、数量和长度限制 | 后端 | 阻塞表单校验 |
| META-Q-003 | TO_CONFIRM | 批量更新是否支持部分更新、覆盖、清空和部分成功 | 后端+QA | 阻塞批量交互 |
| META-Q-004 | PARTIAL | 过滤结构复用瀚海条件组；完整类型和运算符枚举待后端给出 | 后端/检索 | 阻塞表单校验 |
| META-Q-005 | TO_CONFIRM | 内置字段是否存在、是否可开关、是否只读 | 产品+后端 | 阻塞 P1 范围 |
| META-Q-006 | TO_CONFIRM | 星流适配层的 Action 名称、版本和上线顺序 | 平台后端 | 阻塞接口联调 |

## 13. 评审记录

详见 `评审记录.md`。当前版本可进入产品/研发初评，不能作为最终开发闭环。
