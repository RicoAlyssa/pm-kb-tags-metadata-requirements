# 待确认问题：知识库新增元数据接口

状态：评审稿。以下问题不再是“接口缺失”，而是需要后端评审拍板的实现参数。

| ID | 问题 | 建议默认值 | 负责人 | 阻塞阶段 |
|---|---|---|---|---|
| META-CONFIRM-001 | 星流最终 Action 名称是否采用契约中的 `CreateMetadataField` 等命名 | 采用本文命名，若后端改名需保持语义和字段不变 | 平台后端 | 联调 |
| META-CONFIRM-002 | P0 字段类型是否只开放 `String` | 是；其他类型后续扩展 | 产品+后端 | 前端控件 |
| META-CONFIRM-003 | 字段名称长度、字段数量、值长度限制 | 名称 1-40 字符；字段 100 个；值 0-500 字符 | 后端+QA | 校验 |
| META-CONFIRM-004 | 删除已填写但未被检索引用的字段是否允许 | 允许，删除字段和文档值，并触发索引刷新 | 后端+检索 | 删除验收 |
| META-CONFIRM-005 | 批量编辑最大文档数 | 100 | 后端+QA | 批量验收 |
| META-CONFIRM-006 | `MetadataFilter` 是否以前端 `MetadataId` 为主 | 是，适配层映射到底层字段名 | 检索后端 | 检索联调 |
| META-CONFIRM-007 | 元数据更新后检索生效 SLA | 目标 1 分钟内；超时展示异常态 | 检索后端 | 发布 |
