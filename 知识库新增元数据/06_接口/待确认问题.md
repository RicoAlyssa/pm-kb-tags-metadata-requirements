# 待确认问题：元数据接口

| ID | 问题 | 负责人 | 阻塞阶段 | 下一动作 |
|---|---|---|---|---|
| META-API-Q-001 | 瀚海 OPEN-API 的元数据能力是否已经接入或计划接入星流平台 API Explorer/控制台？上线版本与网关路径是什么？ | 后端+平台 | 接口/原型/PRD | 对齐瀚海接口与当前平台 OpenAPI 命名、鉴权和发布计划 |
| META-API-Q-002 | 元数据定义是知识库级 schema，还是文档可临时新增 key？ | 产品+后端 | 产品定义/API | 基于查询接口 `doc_metadata[]` 初步判断为知识库级 schema，需后端确认 |
| META-API-Q-003 | 新增元数据定义支持哪些类型？字段名称规则、唯一性、长度、数量限制是什么？ | 后端 | PRD/前端 | `/473610411e0` 已确认 `name/type` 字段，枚举与校验待补 |
| META-API-Q-004 | 更新元数据定义是否只能改名？类型能否修改？ | 后端 | PRD | `/473619166e0` 仅见 `name`，`type` 是否不可改待确认 |
| META-API-Q-005 | 删除元数据定义是否删除所有文档值？被检索配置引用时如何处理？ | 后端+产品 | PRD/验收 | 读取 `/473629415e0` 并问检索负责人 |
| META-API-Q-006 | 查询元数据定义是否需要分页、排序、搜索？`count` 的统计口径是什么？ | 后端 | 前端/API | `/473713180e0` 已确认 `doc_metadata[].count`，统计口径待确认 |
| META-API-Q-007 | 更新文档元数据中的 `id/name/value` 是否都必填？`value` 支持哪些类型？ | 后端 | 前端/API | 补齐 `/473631326e0` schema |
| META-API-Q-008 | 批量更新是否支持部分成功？失败明细字段是什么？ | 后端+QA | 验收 | 查响应 schema/错误码 |
| META-API-Q-009 | 空值、null、缺失字段分别表示忽略、清空还是报错？ | 后端 | 前端/API | 查接口说明或发授权测试 |
| META-API-Q-010 | 当前平台 `RetrieveKnowledge` 未暴露 metadata filter，后续是否复用瀚海 `metadata_filtering_conditions`？ | 后端/检索 | PRD/验收 | 对齐星流平台检索接口升级方案 |
| META-API-Q-011 | 元数据更新后多久影响索引和检索？ | 后端/检索 | 发布/验收 | 明确一致性 SLA |
