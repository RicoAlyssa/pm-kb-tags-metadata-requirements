# 接口清单：元数据

来源：瀚海 OPEN-API（2026-07-09 浏览器验证）

## 已发现接口

| 能力 | 方法 | Apifox 文档 | 状态 |
|---|---|---|---|
| 新增元数据 | POST | `/473610411e0` | 路径/字段待提取 |
| 更新元数据 | PATCH | `/473619166e0` | 路径/字段待提取 |
| 删除元数据 | DELETE | `/473629415e0` | 路径/字段待提取 |
| 查询元数据 | GET | `/473713180e0` | 路径/响应待提取 |
| 更新文档元数据 | POST | `/473631326e0` | 已提取主要请求结构 |

文档入口：
`https://s.apifox.cn/c15467f3-ecc4-4cca-9c98-a414f418f621/473631326e0`

## 更新文档元数据

- 路径：
  `http://openapi-hanhai-dev.ai.kingsoft.com/knowledge/openapi/v2/datasets/{dataset_id}/documents/metadata`
- 路径参数：`dataset_id`，UUID，必填。
- 请求体：
  - `operation_data[]`
  - `operation_data[].document_id`
  - `operation_data[].metadata_list[]`
  - 示例中的元数据项含 `id`、`value`、`name`
- 已见响应：`200`（无响应体）、`400`、`401`、`403`、`404`。

## 正式竞品接口/文档证据：火山云与阿里云

| 竞品 | 能力 | 关键字段/语义 | 证据 |
|---|---|---|---|
| 火山云/火山引擎 | 元数据更新 | API 目录包含 `update_meta` | `https://docs.volcengine.com/docs/84313/1269158?lang=zh` |
| 火山云/火山引擎 | 文档字段/标签更新 | 导入时传入标签字段，或上传后通过更新文档信息接口设置标签字段 | `https://docs.volcengine.com/docs/84313/2192050?lang=zh` |
| 火山云/火山引擎 | 检索过滤 | `doc_filter` 用于配置标签权限过滤 | `https://docs.volcengine.com/docs/84313/2192050?lang=zh` |
| 阿里云百炼 | SearchFilters | 过滤召回文本切片；多个子分组默认 AND 且不可更改 | `https://help.aliyun.com/zh/model-studio/how-to-use-search-filters` |
| 阿里云 PAI | MetaDataFilterConditions | `FilterCondition=and/or`；`Operator` 支持 `==`、`!=`、`contains`（contains 仅 `file_name`） | `https://help.aliyun.com/zh/pai/developer-reference/api-pailangstudio-2024-07-10-retrieveknowledgebase` |

## 参考实现：Dify Metadata API

> 仅作为非竞品参考实现，不等同于正式竞品证据，也不等同于我方字段。

| 能力 | 方法/路径 | 关键字段/语义 | 证据 |
|---|---|---|---|
| 创建 Metadata Field | `POST /datasets/{dataset_id}/metadata` | `name`、`type`；类型公开为 `string/number/time` | `https://docs.dify.ai/en/api-reference/metadata/create-metadata-field` |
| 查询 Metadata Fields | `GET /datasets/{dataset_id}/metadata` | 响应含 `doc_metadata[]`、`built_in_field_enabled` | `https://docs.dify.ai/en/api-reference/metadata/list-metadata-fields` |
| 更新 Metadata Field | `PATCH /datasets/{dataset_id}/metadata/{metadata_id}` | `name`；重命名自定义字段 | `https://docs.dify.ai/en/api-reference/metadata/update-metadata-field` |
| 获取内置字段 | `GET /datasets/{dataset_id}/metadata/built-in` | 系统内置字段列表 | `https://docs.dify.ai/en/api-reference/metadata/get-built-in-metadata-fields` |
| 启用/禁用内置字段 | `POST /datasets/{dataset_id}/metadata/built-in/{action}` | `action=enable/disable` | `https://docs.dify.ai/en/api-reference/metadata/update-built-in-metadata-field` |
| 批量更新文档元数据 | `POST /datasets/{dataset_id}/documents/metadata` | `operation_data[].document_id`、`metadata_list[].id/name/value`、`partial_update` | `https://docs.dify.ai/en/api-reference/metadata/update-document-metadata-in-batch` |

## 我方接口待补清单

| API ID | 能力 | 必须补齐的信息 | 状态 |
|---|---|---|---|
| META-API-001 | 新增元数据定义 | 方法/路径、字段名、类型枚举、名称规则、响应体、错误码 | TO_CONFIRM |
| META-API-002 | 更新元数据定义 | 是否只允许改名；类型是否可改；被使用/被检索引用时处理 | TO_CONFIRM |
| META-API-003 | 删除元数据定义 | 删除影响范围、引用阻断、响应与错误码 | TO_CONFIRM |
| META-API-004 | 查询元数据定义 | 响应结构、使用文档数、内置字段、分页/排序 | TO_CONFIRM |
| META-API-005 | 更新文档元数据 | `id/name/value` 类型、必填性、空值、批量原子性、partial update | PARTIAL |
| META-API-006 | 检索 metadata filter | 请求表达式、运算符、AND/OR、权限组合、空值语义 | TO_CONFIRM |

## 证据边界

- 上述内容来自只读文档浏览，尚未向真实环境发请求。
- `id/name/value` 的必填性、类型、空值语义、批量原子性、数量限制、
  检索生效时间仍为 `TO_CONFIRM`。
- 接口文档访问凭证不得写入项目文件、测试、Skill 或 Git。
