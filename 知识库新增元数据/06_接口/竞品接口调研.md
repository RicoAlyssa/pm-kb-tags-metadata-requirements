# 竞品接口调研：知识库新增元数据

状态：进行中  
正式竞品：火山云/火山引擎、阿里云/百炼/PAI  
边界：Apifox/瀚海 OPEN-API 是我方底层接口源；金山云 API Explorer 是当前星流平台开放接口源；正式竞品只记录火山云/火山引擎、阿里云/百炼/PAI 的官方 API、官方文档与授权控制台/HAR 证据。

## 1. 结论先行

1. 元数据接口必须同时看“定义/赋值/检索过滤”三段。
   - 瀚海 OPEN-API 已看到元数据定义和文档元数据更新能力。
   - 金山云当前星流平台 API Explorer 尚未暴露同等元数据定义/赋值/过滤能力。
   - 竞品接口重点在“检索时怎么表达过滤条件”，这会反推 PRD 里的交互状态表。

2. 阿里云给出了两套有价值的接口形态。
   - 百炼 Model Studio：`Retrieve` 请求体里的 `searchFilters`，适合字段过滤和标签过滤。
   - PAI LangStudio：`RetrieveKnowledgeBase` 的 `MetaDataFilterConditions`，显式支持 `and/or` 与操作符。

3. 火山当前有官方文档证据，但字段级请求体仍需 HAR/详情页补齐。
   - API 列表明确包含文档 `update_meta`，且说明文档 meta 信息更新会触发索引数据更新。
   - 标签配置和知识检索官方文档可证明“标签定义/文档打标/检索过滤”的产品链路，但字段级请求体仍需授权 Network/HAR 补证。

## 2. 竞品接口证据表

| Evidence ID | 竞品 | 能力 | 接口/参数/结构 | 状态 | 证据来源 |
|---|---|---|---|---|---|
| META-CAPI-VOLC-001 | 火山云/火山引擎 | 强类型字段定义 | 控制台“文档标签”支持字段名、字段类型、标签选项；类型含 String、List、数值、Bool、Date_time、Geo_point 等 | VERIFIED（控制台 UI） | `../02_竞品调研/evidence/火山方舟控制台采证摘要.md` |
| META-CAPI-VOLC-002 | 火山云/火山引擎 | 内置可过滤字段 | 控制台说明 `doc_id` 可在检索时过滤，无需配置 | VERIFIED（控制台 UI） | `../02_竞品调研/evidence/火山方舟控制台采证摘要.md` |
| META-CAPI-VOLC-003 | 火山云/火山引擎 | 元数据更新 | API 列表包含文档 `update_meta`，说明文档 meta 信息更新会自动触发索引中的数据更新 | VERIFIED（官方文档 API 列表） | `https://docs.volcengine.com/docs/82379/1511946?lang=zh` |
| META-CAPI-VOLC-004 | 火山云/火山引擎 | 标签/元数据过滤 | 知识检索参数说明包含“按标签过滤文档范围”，条件含包含、不包含、大于、小于、大于等于、小于等于，关系含且/或 | VERIFIED（官方文档） | `https://docs.volcengine.com/docs/82379/1353778?lang=zh` |
| META-CAPI-ALI-001 | 阿里云百炼 | Retrieve `SearchFilters` | 请求体含 `indexId`、`query`、`searchFilters`；用于过滤语义检索结果 | VERIFIED（官方文档） | `https://help.aliyun.com/zh/model-studio/how-to-use-search-filters` |
| META-CAPI-ALI-002 | 阿里云百炼 | SearchFilters 语法 | 子分组之间默认 AND 且不可更改；支持单值、多值、范围、模糊、标签查询 | VERIFIED（官方文档） | `https://help.aliyun.com/zh/model-studio/how-to-use-search-filters` |
| META-CAPI-ALI-003 | 阿里云 PAI | RetrieveKnowledgeBase 元数据过滤 | `POST /api/v1/langstudio/knowledgebases/{KnowledgeBaseId}/action/retrieve`；请求体含 `MetaDataFilterConditions` JSON 字符串 | VERIFIED（官方文档） | `https://help.aliyun.com/zh/pai/developer-reference/api-pailangstudio-2024-07-10-retrieveknowledgebase` |

## 2.1 我方当前平台接口边界

| Evidence ID | 来源 | 能力 | 结论 | 状态 | 证据来源 |
|---|---|---|---|---|---|
| META-OUR-KSYUN-001 | 金山云 API Explorer / 星流平台 | 知识库接口列表 | 已公开 `RetrieveKnowledge`、`ImportDocuments`、`DescribeDocuments`、`DescribeDocument` 等；未见元数据定义 CRUD、文档元数据赋值接口 | VERIFIED | `https://apiexplorer.ksyun.com/#/document/documentList/239/1246` |
| META-OUR-KSYUN-002 | 金山云 API Explorer / 星流平台 | `DescribeDocument.Metadata` | 字段语义为返回内容粒度 `all / only / without`，不是客户自定义元数据字段 | VERIFIED | `https://apiexplorer.ksyun.com/#/document/documentDetail/239/DescribeDocument/2025-11-14/1246` |
| META-OUR-KSYUN-003 | 金山云 API Explorer / 星流平台 | `RetrieveKnowledge` | 当前请求参数未见 metadata filter；只见 `DatasetId`、`Query`、`RetrievalModel`、`TopK`、`ScoreThreshold` 等 | VERIFIED | `https://apiexplorer.ksyun.com/#/document/documentDetail/239/RetrieveKnowledge/2025-11-14/1246` |

判断：瀚海 API 已有底层能力，但当前平台开放接口还没有同等能力；PRD 应写成“接入/暴露底层能力”，不是“当前接口已支持”。

## 3. 阿里云接口要点

### 3.1 Model Studio Retrieve + SearchFilters

官方示例中的最小结构：

```json
{
  "indexId": "o73yjlxxxx",
  "query": "公司中姓名为张三的员工",
  "searchFilters": [
    {
      "姓名": "张三"
    }
  ]
}
```

已确认语义：

- `SearchFilters` 用于在语义检索结果上继续过滤，尤其适合结构化数据。
- `searchFilters` 可包含多个子分组。
- 子分组之间默认 AND，且不可更改。
- 子分组内字段支持：
  - 单值查询：数值、字符串。
  - 多值查询：纯数值数组或纯字符串数组。
  - 范围查询：`eq`、`neq`、`gt`、`gte`、`lt`、`lte`。
  - 模糊查询：字符串 `like`。
  - 标签查询：仅文档搜索、音视频搜索类知识库。

对我方影响：

- 元数据过滤 PRD 必须写清楚运算符、值类型和组合关系。
- 如果我方后端只支持 AND，应在 UI 上显式提示；如果支持 OR，应比阿里更灵活。
- 元数据字段“是否参与检索”不只是展示字段，而是决定是否可进入过滤表达式。

### 3.2 PAI RetrieveKnowledgeBase + MetaDataFilterConditions

官方接口形态：

```http
POST /api/v1/langstudio/knowledgebases/{KnowledgeBaseId}/action/retrieve
```

核心请求字段：

| 字段 | 语义 |
|---|---|
| `WorkspaceId` | 知识库所在工作空间 ID |
| `Query` | 检索内容 |
| `TopK` | 返回排名最高的结果数 |
| `ScoreThreshold` | 相似度分数阈值，范围 `[0, 1]` |
| `MetaDataFilterConditions` | 元数据过滤条件，JSON 字符串 |
| `QueryMode` | `dense` 或 `hybrid` |

`MetaDataFilterConditions` 结构：

```json
{
  "FilterCondition": "and",
  "MetaDataFilters": [
    {
      "Key": "key1",
      "Value": "value1",
      "Operator": "=="
    },
    {
      "Key": "key2",
      "Value": "value2",
      "Operator": "!="
    },
    {
      "Key": "file_name",
      "Value": "prefix",
      "Operator": "contains"
    }
  ]
}
```

已确认语义：

- `FilterCondition` 支持 `and`、`or`。
- `Operator` 支持 `==`、`!=`、`contains`。
- `contains` 仅支持 `file_name` 字段。

对我方影响：

- 我方检索接口建议采用显式结构，而不是让前端拼 JSON 字符串。
- 如果为了快速兼容后端使用 JSON 字符串，也要在 PRD 写清楚前端如何校验和展示错误。
- 内置字段如文件名、文档 ID、来源 URL 应和客户自定义字段分层展示。

## 4. 火山接口/控制台要点

火山当前已验证的是 UI 与交互结构：

- 文档标签强类型字段非常接近元数据 Schema。
- `doc_id` 是无需配置即可用于检索过滤的内置字段。
- 检索/问答页面都存在按标签筛选能力。

官方 API/文档证据：

- `https://docs.volcengine.com/docs/82379/1511946?lang=zh`：API 列表包含文档 `update_meta`，且说明文档 meta 信息更新会自动触发索引数据更新。
- `https://docs.volcengine.com/docs/82379/1261891?lang=zh`：标签配置说明创建知识库时初始化标签、进入知识库详情原始文档页面后可编辑文档标签，标签配置后可用于检索过滤。
- `https://docs.volcengine.com/docs/82379/1353778?lang=zh`：知识检索参数说明支持按标签过滤文档范围，过滤条件包含/不包含/大于/小于/大于等于/小于等于，条件关系为且/或。

仍需补证：火山字段级请求/响应、错误码、权限码、批量语义，需要授权控制台 Network/HAR。

下一轮必须抓的火山接口证据：

| 场景 | 需要抓取的请求 | 用途 | 脱敏要求 |
|---|---|---|---|
| 创建文档标签字段 | 创建知识库高级配置提交请求 | 确认字段类型枚举、字段名规则、选项结构 | 去除租户、账号、资源 ID |
| 编辑文档标签/元数据值 | 原始文档标签保存请求 | 确认文档级值更新结构 | 去除真实文档内容 |
| 检索筛选 | 知识检索/问答筛选请求 | 确认过滤表达式是否为 `doc_filter` 或其他字段 | 去除 Query、Cookie、Authorization |
| `update_meta` 官方文档 | 浏览器打开官方 API 页并截图/摘录 | 确认方法、路径、请求体、响应和错误码 | 不保存账号凭证 |

## 5. 我方接口拆分建议

| 我方 API 能力 | 对标来源 | 必须表达的语义 | 状态 |
|---|---|---|---|
| 元数据定义查询/新增/更新/删除 | 瀚海 OPEN-API + 火山强类型字段 UI | 字段 Key、名称、类型、可选项、是否参与检索、是否参与模型回复 | PARTIAL |
| 文档元数据值更新 | 瀚海 OPEN-API 更新文档元数据 | 单文档/批量、覆盖/部分更新、空值/清空语义、部分失败 | PARTIAL |
| 检索 metadata filter | 阿里 `SearchFilters` + PAI `MetaDataFilterConditions` | AND/OR、运算符、值类型、字段可用性、无命中反馈 | TO_CONFIRM |
| 内置元数据字段 | 火山 `doc_id` + 阿里 `file_name` contains 限制 | 只读字段、是否默认可检索、是否可展示/隐藏 | TO_CONFIRM |
| 接入当前星流平台 | 金山云 API Explorer 当前接口缺口 | 网关路径、OpenAPI Action 命名、鉴权、版本、错误码、控制台入口 | TO_CONFIRM |

## 6. 待确认问题

| ID | 问题 | 负责人 | 阻塞阶段 |
|---|---|---|---|
| META-CAPI-Q-001 | 我方检索接口是否已有 metadata filter，字段名和表达式是什么？ | 检索后端 | PRD/API |
| META-CAPI-Q-002 | 元数据定义字段类型是否覆盖 String、List、Number、Bool、DateTime；是否有 GeoPoint？ | 后端 | PRD/前端 |
| META-CAPI-Q-003 | 文档元数据更新是全量覆盖、部分更新，还是由字段 `partial_update` 控制？ | 后端 + QA | 验收 |
| META-CAPI-Q-004 | 元数据更新后多久影响检索索引？是否需要重建索引/重切片？ | 检索后端 | 发布/验收 |
| META-CAPI-Q-005 | 内置字段 `doc_id`、`file_name`、`source_url` 是否默认可过滤，是否允许用户禁用？ | 产品 + 后端 | 交互/API |
| META-CAPI-Q-006 | 瀚海元数据接口如何上线到星流平台当前 OpenAPI/控制台？ | 平台后端 | 接口/发布 |
